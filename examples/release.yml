name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: devjskit/github-workflows/.github/workflows/release.yml@v1
    with:
      publish: true
    permissions:
      contents: write
      id-token: write

  # release-jsr:
  #   uses: devjskit/github-workflows/.github/workflows/release-jsr.yml@v1
  #   permissions:
  #     contents: read
  #     id-token: write
